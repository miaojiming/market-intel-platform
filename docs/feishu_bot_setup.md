# 飞书机器人配置指南（长连接模式）

## 为什么用长连接？

* **不需要公网域名** — 服务主动连接飞书服务器

* **部署更简单** — 不用配置 Webhook、不用 SSL 证书

* **消息实时性好** — WebSocket 长连接，毫秒级响应

* **更安全** — 不需要暴露端口到公网

## 前置条件

* 已部署服务（Railway / Docker / 自建均可）

* 有飞书开发者后台权限

***

## 步骤 1：创建飞书企业自建应用

1. 打开 <https://open.feishu.cn/app>
2. 点击 **"创建企业自建应用"**
3. 填写：

   * 应用名称：市场情报助手

   * 应用描述：收单&银行IT行业情报 + 客户画像查询
4. 点击 **"创建"**

## 步骤 2：获取凭证

在应用详情页 → **"凭证与基础信息"**：

* 复制 **App ID**（如 `cli_xxxxxxxxxx`）

* 复制 **App Secret**（如 `xxxxxxxxxxxxxxxxxxxx`）

配置到环境变量：

```
FEISHU_APP_ID=cli_xxxxxxxxxx
FEISHU_APP_SECRET=xxxxxxxxxxxxxxxxxxxx
```

## 步骤 3：添加权限

左侧菜单 → **"权限管理"** → 搜索并添加以下权限：

| 权限名称         | 权限码                       | 用途          |
| ------------ | ------------------------- | ----------- |
| 获取与发送单聊、群组消息 | `im:message`              | 发消息/卡片      |
| 读取群消息        | `im:message.group_at_msg` | 接收群里@机器人的消息 |
| 获取群组信息       | `im:chat`                 | 获取群信息       |
| 以应用身份读取会话    | `im:message:send_as_bot`  | 以机器人身份发消息   |

添加后点 **"批量申请"**，提交审批（管理员审批后生效）。

## 步骤 4：开启长连接模式

左侧菜单 → **"事件与回调"** → **"长连接模式"**

1. 点击 **"启用长连接模式"**
2. 系统会提示："无需注册公网域名或配置加密策略"
3. 确认启用

> 注意：启用长连接模式后，HTTP 回调会自动失效。我们的服务会主动连飞书服务器。

## 步骤 5：添加事件

在"长连接模式"页面：

1. 点 **"添加事件"**
2. 搜索并添加：**接收消息**（`im.message.receive_v1`）
3. 保存

## 步骤 6：创建版本并发布

左侧菜单 → **"版本管理与发布"** → **"创建版本"**

* 版本号：1.0.0

* 更新说明：初始版本

* 可用性：全员可用 / 指定群组

提交发布，等待管理员审批。

## 步骤 7：把机器人拉进群

1. 在飞书里打开目标群
2. 群设置 → 群机器人 → 添加机器人
3. 搜索"市场情报助手"添加

## 步骤 8：验证

服务启动后，在群里测试：

```
@情报助手 星展银行
```

或者：

```
/profile 招商银行
```

几秒后会收到客户画像卡片。

发送 `/help` 查看帮助信息。

***

## 环境变量清单

| 变量                   | 必填 | 说明                             |
| -------------------- | -- | ------------------------------ |
| `FEISHU_APP_ID`      | ✅  | 飞书应用 App ID                    |
| `FEISHU_APP_SECRET`  | ✅  | 飞书应用 App Secret                |
| `FEISHU_WEBHOOK_URL` | ❌  | 群机器人 Webhook（用于每日情报推送，可选）      |
| `LLM_API_KEY`        | ✅  | LLM Gateway API Key            |
| `LLM_BASE_URL`       | ✅  | `https://llm-gw.agenzo.com/v1` |
| `LLM_MODEL`          | ✅  | `kiro/claude-opus-5`           |

***

## 常见问题

### Q: 长连接需要服务器一直开着吗？

A: 是的。服务启动时长连接自动建立，服务关闭连接断开。Railway 部署的话，注意不要让服务休眠（Hobby 计划及以上不休眠）。

### Q: 消息收不到怎么办？

A: 检查：

1. 服务日志里有没有 "长连接已启动"
2. 权限有没有申请并通过审批
3. 机器人有没有被拉进群
4. 事件有没有添加 "接收消息"

### Q: 长连接会断吗？

A: SDK 内置了自动重连机制（`auto_reconnect=True`），网络波动会自动重连，不用管。
